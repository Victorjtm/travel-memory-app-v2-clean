import { ActividadesDisponibles } from './actividades-disponibles.model';

describe('ActividadesDisponibles', () => {
  it('should create an instance', () => {
    expect(new ActividadesDisponibles()).toBeTruthy();
  });
});
